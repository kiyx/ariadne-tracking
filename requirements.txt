# ══════════════════════════════════════════════
# Ariadne Tracking — Dipendenze pip
# ══════════════════════════════════════════════
# NOTA: PyTorch va installato PRIMA e SEPARATAMENTE
# con il comando specifico per la tua GPU:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

# Framework YOLO (include BoT-SORT, pre/post-processing, export)
ultralytics>=8.3.0

# Computer Vision (lettura video, resize, salvataggio immagini)
opencv-python>=4.8.0

# Numerica
numpy>=1.24.0

# Barra di progresso nel terminale
tqdm>=4.66.0

# Parsing configurazione YAML
pyyaml>=6.0

# Download dataset MEVID
requests>=2.28.0

# Re-Identification
yacs>=0.1.8

# Logging avanzato (colori, tabelle, progress bar)
rich>=13.0.0
